<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HangedGame" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HangedGame" parent="Base.Theme.HangedGame" />

    <style name="btnLetter">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">30dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@drawable/rounded_button</item>
<!--        <item name="android:fontFamily">@font/caveat</item>-->
        <item name="android:text">@string/a</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="btnLetterFirstRow" parent="btnLetter">
        <item name="android:layout_marginEnd">3dp</item>
    </style>

    <style name="btnLetterSecondRow" parent="btnLetter">
        <item name="android:layout_marginEnd">4dp</item>
    </style>

    <style name="btnLetterThirdRow" parent="btnLetter">
        <item name="android:layout_marginEnd">7dp</item>
    </style>
</resources>